# Portable core only: no TrueFoundry-specific infrastructure assumed.
# This is the shape a cluster outside TrueFoundry's own fleet would install.
clusterName: tfy-example-minimal
recorder:
  enabled: false
