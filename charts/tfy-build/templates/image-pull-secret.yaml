{{- if (tpl .Values.imagePullCredentials .) -}}
apiVersion: v1
data:
  .dockerconfigjson: {{ tpl .Values.imagePullCredentials . }}
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}-image-pull-secret
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
{{- end }}