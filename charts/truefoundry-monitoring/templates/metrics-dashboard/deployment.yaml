{{- if .Values.tfyMetricsDashboard.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "truefoundry-monitoring.metrics-dashboard.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "truefoundry-monitoring.metrics-dashboard.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  replicas: {{ .Values.tfyMetricsDashboard.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "truefoundry-monitoring.metrics-dashboard.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "truefoundry-monitoring.metrics-dashboard.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "truefoundry-monitoring.metrics-dashboard.serviceAccountName" . }}
      {{- with .Values.tfyMetricsDashboard.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: tfy-metrics-dashboard
          image: {{ include "truefoundry-monitoring.metrics-dashboard.image" . | quote }}
          imagePullPolicy: {{ .Values.tfyMetricsDashboard.imagePullPolicy }}
          {{- with .Values.tfyMetricsDashboard.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: PROMETHEUS_URL
              value: {{ .Values.tfyMetricsDashboard.prometheusUrl | default "" | quote }}
            - name: TARGET_NAMESPACE
              value: {{ .Values.tfyMetricsDashboard.targetNamespace | default (include "truefoundry-monitoring.controlPlaneNamespace" .) | quote }}
            - name: DASHBOARD_VISIBILITY
              value: {{ .Values.tfyMetricsDashboard.dashboardVisibility | toJson | quote }}
          ports:
            - name: http
              containerPort: {{ .Values.tfyMetricsDashboard.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            {{- include "truefoundry-monitoring.metrics-dashboard.resources" . | nindent 12 }}
      imagePullSecrets:
        {{- include "truefoundry-monitoring.imagePullSecrets" (list .Values.tfyMetricsDashboard.imagePullSecrets .) | nindent 8 }}
      {{- with .Values.tfyMetricsDashboard.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tfyMetricsDashboard.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tfyMetricsDashboard.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
