{{- if .Values.serviceMonitors.tfyOtelCollector.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Values.serviceMonitors.tfyOtelCollector.name }}
  labels:
    {{- include "otelCollector.labels" . | nindent 4 }}
  annotations:
    {{- include "otelCollector.annotations" . | nindent 4 }}
spec:
  jobLabel: {{ .Values.serviceMonitors.tfyOtelCollector.jobLabel }}
  endpoints:
  {{- toYaml .Values.serviceMonitors.tfyOtelCollector.endpoints | nindent 2 }}
  namespaceSelector:
    {{- toYaml .Values.serviceMonitors.tfyOtelCollector.namespaceSelector | nindent 4 }}
  selector:
    matchLabels:
      {{- toYaml .Values.serviceMonitors.tfyOtelCollector.serviceSelectorLabels | nindent 6 }}
{{- end -}}