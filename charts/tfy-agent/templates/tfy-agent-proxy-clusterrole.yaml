{{- if .Values.tfyAgentProxy.enabled -}}
{{- if .Values.tfyAgentProxy.clusterRole.enable -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "tfy-agent-proxy.fullname" . }}
rules:
    {{- .Values.tfyAgentProxy.clusterRole.rules | toYaml | nindent 2 }}
{{- end }}
{{- end }}
