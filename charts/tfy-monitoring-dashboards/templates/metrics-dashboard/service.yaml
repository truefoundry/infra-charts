{{- if .Values.tfyMetricsDashboard.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tfy-monitoring-dashboards.metrics-dashboard.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tfy-monitoring-dashboards.metrics-dashboard.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: {{ .Values.tfyMetricsDashboard.service.type }}
  ports:
    - name: http
      port: {{ .Values.tfyMetricsDashboard.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "tfy-monitoring-dashboards.metrics-dashboard.selectorLabels" . | nindent 4 }}
{{- end }}
