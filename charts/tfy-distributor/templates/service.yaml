apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.name }}
spec:
  ports:
    - name: port-3000
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: {{ .Values.global.name }}
  type: ClusterIP
