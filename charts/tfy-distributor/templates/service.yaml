apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-distributor
spec:
  ports:
    - name: port-3000
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
