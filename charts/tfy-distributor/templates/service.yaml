apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-distributor
spec:
  ports:
  {{ range $val := .Values.distributor.ports -}}
    - {{ toYaml $val | nindent 6 }}
  {{ end -}}
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
