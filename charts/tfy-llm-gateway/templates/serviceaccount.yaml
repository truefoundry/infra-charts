{{- if and .Values.serviceAccount.create .Values.serviceAccount.name -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tfy-llm-gateway.serviceAccountName" . }}
  labels:
    {{- include "tfy-llm-gateway.serviceAccountLabels" . | nindent 4 }}
  annotations:
    {{- include "tfy-llm-gateway.serviceAccountAnnotations" . | nindent 4 }}
{{- if hasKey .Values.serviceAccount "automountServiceAccountToken" }}
automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
