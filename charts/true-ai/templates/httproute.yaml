{{- if .Values.httpRoute.enabled -}}
{{- $serviceName := include "true-ai.fullname" . -}}
{{- $servicePort := .Values.service.port -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "true-ai.fullname" . }}
  namespace: {{ include "global.namespace" . }}
  labels:
    {{- include "true-ai.httpRouteLabels" . | nindent 4 }}
  annotations:
    {{- include "true-ai.httpRouteAnnotations" . | nindent 4 }}
spec:
  parentRefs:
    {{- toYaml .Values.httpRoute.parentRefs | nindent 4 }}
  {{- if .Values.httpRoute.hostnames }}
  hostnames:
    {{- range $hostname := .Values.httpRoute.hostnames }}
    - {{ tpl $hostname $ | quote }}
    {{- end }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.httpRoute.path | default "/" }}
      backendRefs:
        - name: {{ $serviceName }}
          port: {{ $servicePort }}
{{- end }}
