# Dummy values so `ct lint` and helm template succeed.
# Installs must still set real googleAdminEmail, scimBaseUrl, and secret.scimToken.
googleAdminEmail: ci-admin@example.com
googleServiceAccountEmail: ci-robot@example.iam.gserviceaccount.com
googleGroupEmails: ci-group@example.com
scimBaseUrl: https://example.truefoundry.tech/api/svc/v1/scim/v2/ci-tenant/ci-sso
secret:
  create: true
  scimToken: ci-not-a-real-token
dryRun: true
