{{- if and .Values.tfyNginxProxy.enabled .Values.tfyNginxProxy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "tfy-nginx-proxy.fullname" . }}
  labels:
    {{- include "tfy-nginx-proxy.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "tfy-nginx-proxy.fullname" . }}
  minReplicas: {{ .Values.tfyNginxProxy.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.tfyNginxProxy.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.tfyNginxProxy.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.tfyNginxProxy.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.tfyNginxProxy.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.tfyNginxProxy.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
