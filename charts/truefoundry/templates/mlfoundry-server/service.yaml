{{- if .Values.mlfoundryServer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlfoundry-server.fullname" . }}
  labels:
    {{- include "mlfoundry-server.serviceLabels" . | nindent 4 }}
  annotations:
    {{- include "mlfoundry-server.serviceAnnotations" . | nindent 4 }}
spec:
  type: {{ .Values.mlfoundryServer.service.type }}
  ports:
    - port: {{ .Values.mlfoundryServer.service.port }}
      targetPort: {{ .Values.mlfoundryServer.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "truefoundry.selectorLabels" (dict "context" . "name" "mlfoundry-server") | nindent 4 }}
{{- end }}
