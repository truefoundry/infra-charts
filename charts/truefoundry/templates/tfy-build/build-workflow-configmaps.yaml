{{- if .Values.tfyBuild.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tfy-build.fullname" . }}-scripts
  labels:
    {{- include "tfy-build.commonLabels" . | nindent 4 }}
  annotations:
    {{- include "tfy-build.commonAnnotations" . | nindent 4 }}
data:
{{ (.Files.Glob "tfy-build-scripts/*").AsConfig | indent 2 }}

{{- end}}