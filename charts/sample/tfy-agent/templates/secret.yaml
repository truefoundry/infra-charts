apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tfy-agent.secretName" . }}
  labels:
    {{- include "tfy-agent.labels" . | nindent 4 }}
stringData:
  token: {{ .Values.config.clusterToken }}  