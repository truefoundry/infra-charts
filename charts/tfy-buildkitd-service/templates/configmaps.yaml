apiVersion: v1
kind: ConfigMap
metadata:
  name: buildkit-config
  labels:
    {{- include "buildkitd-service.labels" . | nindent 4 }}
data:
  buildkitd.toml: |
    {{- .Values.buildkitConfig | nindent 4 }}