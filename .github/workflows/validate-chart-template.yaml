name: Validate Chart Template

on:
  pull_request:
    branches:
      - 'main'
    paths:
      - 'charts/**'
  push:
    branches:
      - 'add-validate-chart-temp-workflow'

jobs:
  validate-chart-template:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Helm
        uses: azure/setup-helm@v4.2.0
        with:
          version: v3.15.1

      - name: Set up chart-testing
        uses: helm/chart-testing-action@v2.7.0

      - name: Run chart-testing (list-changed)
        id: list-changed
        run: |
          changed_charts=$(ct list-changed --target-branch ${{ github.event.repository.default_branch }})
          if [[ -n "$changed_charts" ]]; then
            # Use the EOF syntax so that newlines are preserved
            {
              echo "changed-charts<<EOF"
              echo "$changed_charts"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
      
            echo "changed=true" >> "$GITHUB_OUTPUT"
            fi

#      - name: Run chart-testing (lint)
#        if: steps.list-changed.outputs.changed == 'true'
#        run: |
#          ct lint --target-branch ${{ github.event.repository.default_branch }}

      - name: Echo Changed Charts
        run: |
          echo "Changed charts: ${{ steps.list-changed.outputs.changed-charts }}"

      - name: Validate Chart Templates
        if: steps.list-changed.outputs.changed == 'true'
        run: |
          changed="${{ steps.list-changed.outputs.changed-charts }}"
          echo "🛠️  Validating Helm chart templates for: $changed"
          
          helm repo update

          for chart in $changed; do
            if [ -d "$chart" ]; then
              echo "🛠️  Building dependencies for chart: $chart"
              helm dependency build "$chart"

              echo "🛠️  Rendering chart templates for: $chart"
              helm template "$chart"
              if [ $? -ne 0 ]; then
                echo "❌ helm template failed for $chart."
                exit 1
              else
                echo "✅ Chart template succeeded for $chart."
              fi
            else
              echo "⚠️ Skipping validation for $chart: not a directory."
            fi
          done